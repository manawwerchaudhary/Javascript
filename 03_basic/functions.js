const user={
    username:"manawwer",
    price:199
}

function handleObject(object){
    console.log(`username is ${object.username} and price is ${object.price}`);
    
}
// handleObject(user)
handleObject({
    username:"sam",
    price :399

})